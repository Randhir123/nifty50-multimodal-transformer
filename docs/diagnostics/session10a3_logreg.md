# Session 10a.3 Logreg Diagnostic

Status: pending rerun on a KG v2 artifact.

The implementation now emits 37 KG v2 features by default, but this workspace does not contain the Colab artifact needed to recompute the logreg comparison against the old KG run. Re-run the experiment notebook on the cached raw data folder and save the tabular-only vs tabular+kg_v2 logreg table here.
